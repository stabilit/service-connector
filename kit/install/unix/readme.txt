serviceconnector unix installation

1) install sc files

/home/sc/prod  (production release)
/home/sc/test (test release)
/home/sc/dev (dev release)

2) install daemon script

/etc/init.d/serviceconnector.sh

Note:

The daemon script uses user: <user>. For a general installation a new user must be installed (e.g. sc).

