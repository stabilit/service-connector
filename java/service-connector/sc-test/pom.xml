<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<artifactId>sc-test</artifactId>
	<packaging>jar</packaging>

	<name>SC Test</name>
	<description>Test project for SC</description>
	<url>http://maven.apache.org</url>

	<parent>
		<groupId>org.serviceconnector</groupId>
		<artifactId>sc-parent</artifactId>
		<version>5.2.0-SNAPSHOT</version>
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
	</properties>

	<build>
		<!-- TestConstants.serverRunable/clientRunable launch this jar by name. jar 3.x no
		     longer lets an execution override finalName, so it is set for the whole module;
		     the installed artifact is still sc-test-<version>.jar. -->
		<finalName>test-client-server</finalName>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<configuration>
					<!-- the test client classes are not part of the documented API -->
					<excludePackageNames>org.serviceconnector.cln</excludePackageNames>
					<!-- No doclint on the test harness. It is clean apart from 317 missing-comment
					     warnings, 110 of them on the bare constants in TestConstants. -->
					<doclint>none</doclint>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
							<mainClass>org.serviceconnector.TestMainClass</mainClass>
						</manifest>
					</archive>
				</configuration>
				<executions>
					<execution>
						<!-- The system tests spawn this jar as an OS process during the test
						     phase, i.e. before package, so it has to be built early. attach=false
						     keeps it from taking the place of the module artifact - without it
						     jar 3.x aborts the later default-jar with "You have to use a
						     classifier to attach supplemental artifacts". -->
						<id>create-test-client-server</id>
						<phase>generate-test-resources</phase>
						<goals>
							<goal>jar</goal>
						</goals>
						<configuration>
							<attach>false</attach>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-sc</id>
						<phase>generate-test-resources</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>org.serviceconnector</groupId>
									<artifactId>sc</artifactId>
									<version>${project.version}</version>
									<type>jar</type>
									<overWrite>true</overWrite>
									<outputDirectory>${project.build.directory}</outputDirectory>
									<destFileName>sc.jar</destFileName>
								</artifactItem>
							</artifactItems>
						</configuration>
					</execution>
					<execution>
						<id>copy-dependencies</id>
						<phase>generate-test-resources</phase>
						<goals>
							<goal>copy-dependencies</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<overWriteReleases>false</overWriteReleases>
							<overWriteSnapshots>false</overWriteSnapshots>
							<overWriteIfNewer>true</overWriteIfNewer>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<dependency>
			<groupId>org.serviceconnector</groupId>
			<artifactId>sc-lib</artifactId>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<scope>compile</scope>
		</dependency>
		<dependency>
			<groupId>org.serviceconnector</groupId>
			<artifactId>sc-console</artifactId>
			<scope>compile</scope>
		</dependency>
		<!-- ResourceLeakDetector is used directly by IntegrationSuperTest and SystemSuperTest -->
		<dependency>
			<groupId>io.netty</groupId>
			<artifactId>netty-common</artifactId>
			<scope>compile</scope>
		</dependency>
	</dependencies>

</project>
